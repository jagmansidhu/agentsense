# Copy to `.env` and fill in. Never commit the real .env.

# CLōD LLM API (OpenAI-compatible — see docs/clod_api_quick_reference.md)
CLOD_API_URL=https://api.clod.io/v1/chat/completions
CLOD_MODEL=DeepSeek V3
CLOD_API_KEY=
CLOD_TEMPERATURE=0.7
# Cap reply length. Default is 512 (keeps demo turns short and fast).
# Raise for production / long-form tasks.
# CLOD_MAX_COMPLETION_TOKENS=512

# Comma-separated browser origins allowed to POST the proxy (omit for "*" in dev).
# AGENTSENSE_CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Recommended: point JUDGE_MODEL at a small/fast model from the CLōD catalog
# (e.g. an 8B-class chat model) so the classifier returns in ~1s instead of
# sharing the 70B-class chat model used for agent replies. Leave blank to
# inherit CLOD_MODEL.
JUDGE_MODEL=
# Optional: cap how many recent turns the judge sees. Default 4. Lower =
# faster, higher = better cross-turn loop detection.
# JUDGE_HISTORY_TURNS=4
# Optional: judge output cap. Default 4096; raise if you ever see
# "Judge output truncated".
# JUDGE_MAX_TOKENS=4096

# Greptile (code correlation)
GREPTILE_API_KEY=
GREPTILE_REPO=your-org/your-repo
GREPTILE_BRANCH=main

# OpenClaw (local alert gateway)
OPENCLAW_URL=http://127.0.0.1:18789/api
OPENCLAW_CHANNEL=telegram

# Internal service wiring
CLASSIFIER_URL=http://localhost:8001/classify

# Optional persistence (SQLite). Leave UNSET for hackathon-default in-memory
# mode (zero setup, fastest). Set to a path to persist sessions + events
# across restarts; the file is created on first use.
# AGENTSENSE_DB_PATH=data/agentsense.db
